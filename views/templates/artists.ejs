<% include head %>
  <h1>Artists</h1>
  <form class="artistSearch" action="/artists/search" method="GET">
    Search an Artist by name:
    <input type='text' name="name">
    <input type="submit" value="Search">
  </form>
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Genre</th>
        <th>Origin</th>
      </tr>
    </thead>
    <tbody>
      <% artists.forEach(function (artist) { %>
        <tr>
          <td><%= artist.name %></td>
          <td><%= artist.genre %></td>
          <td><%= artist.origin %></td>
        </tr>
      <% }); %>
    </tbody>
  </table>
<% include foot %>
